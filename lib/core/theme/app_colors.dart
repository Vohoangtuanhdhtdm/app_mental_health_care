import 'package:flutter/material.dart';

class AppColors {
  /// Seed chính (light sky blue, calm)
  static const seed = Color(0xFF8ECDF3);

  // Accent/brand thường dùng nhanh
  static const brand = Color(0xFF78C4F0);
  static const navy = Color(0xFF0B1B2B);

  // Light surfaces
  static const background = Color(0xFFF6F9FC);
  static const surface = Colors.white;
  static const surfaceVariant = Color(0xFFEFF4F9);

  // Dark surfaces
  static const darkBackground = Color(0xFF0E1824);
  static const darkSurface = Color(0xFF111D2A);
  static const darkSurfaceVariant = Color(0xFF142233);

  // Feedback
  static const success = Color(0xFF4FC99A);
  static const warning = Color(0xFFF6C36B);
  static const error = Color(0xFFEF6B6B);
}
